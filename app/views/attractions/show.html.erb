<%  attraction.name %></h1>

% if @user.admin == true %>
<%= link_to 'Edit Attraction', edit_attraction_path(@attraction), class: "btn btn-primary" %><br><br>
<%else %>
<%link_to 'Go to this ride', take_ride_path(@attraction), method: :post, class:"btn btn-primary" %>
<% end %>
